"""
API layer.

Everything specific to testing RiveAr's HTTP API: domain clients
(api/clients/) built on core.api.api_client.ApiClient, and the Pydantic
response contract models they validate against (api/models/).
"""
