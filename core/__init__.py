"""
Core package.

Framework-wide building blocks shared across every layer, split into
per-layer subpackages - today just core/api (HTTP client). core/ui
(page-object base) and core/db (database access base) will be added
alongside ui/ and db/ once those layers exist.
"""
