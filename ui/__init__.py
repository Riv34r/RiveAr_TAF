"""
UI layer.

Everything specific to testing RiveAr's front end with Playwright: Page
Objects, one per screen (ui/pages/), and wrappers for components that appear
on more than one screen (ui/components/).
"""
